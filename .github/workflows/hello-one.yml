name: Hello WF
on: 
  workflow_dispatch:

jobs:
  manual-approval:
    runs-on: ubuntu-latest
    environment: t
    
  hello-one:
    runs-on: ubuntu-latest
    environment: test
    steps:
    - run: echo "Hello One"
